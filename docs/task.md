# Fixture Task

Change the sentence in `src/message.txt`, add one short note under `docs/`, and
leave every policy and credential boundary unchanged.

The expected contribution is intentionally small. A smoke run must still review
the complete diff and explicitly confirm branch publication and pull-request
creation.
